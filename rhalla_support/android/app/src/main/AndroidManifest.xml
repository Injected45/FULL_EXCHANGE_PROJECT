<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      ⚠ INTERNET في `main` لا في `src/debug` وحده.

      قالبُ Flutter يُعلنها في `debug/` و`profile/` فقط، فيخرج إصدارُ الإنتاج
      بلا إذنِ شبكةٍ إطلاقاً — يبني ويوقّع ويُثبَّت، ثم يفشل عند أوّل نداء.
      وهذا وقع فعلاً في تطبيق الوكيل وكلّف إصداراً كاملاً، والتكرارُ هنا كان
      مضموناً لولا هذا السطر.
    -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:label="الرحالة للدعم"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
