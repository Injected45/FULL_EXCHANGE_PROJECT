library;

import 'package:flutter/widgets.dart';

/// مقاسات مسار الحوالة الداخلية: الإدخال ← المراجعة ← «تمّت الحوالة».
///
/// قرار المالك (1 سبتمبر 2026): تظهر هذه الشاشات كاملةً على جهاز متوسّط بلا
/// تمرير لأعلى ولا لأسفل، وبلا حذف أو إخفاء أي حقل أو معلومة.
///
/// المقاس الحاكم **360×640 dp** — ما تعطيه Oppo A37 و A57 (720×1280 عند
/// كثافة 320). و Note 10 أوسع وأطول (411×868) فيفضل عنها هامش. القياس على
/// الأصغر لأن نجاحه يعني نجاح الثلاثة، والعكس غير صحيح.
///
/// هذه القيم أصغر من قيم بقية التطبيق عمداً: شاشات هذا المسار أكثفها
/// محتوىً. لا تُنثر أرقام مباشرة في شجرات البناء — تغييرُ واحدةٍ هنا يعيد
/// ضبط المسار كلّه، و`test/send_internal_screens_fit_test.dart` يقيس النتيجة
/// فيمنع أي عودة صامتة للتمرير.

/// بين البطاقات — بدل `R.gapCard` (12).
const kGap = 6.0;

/// بين عنوان الحقل وحقله.
const kGapLabel = 6.0;

/// حول الفاصل داخل بطاقة.
const kGapRule = 8.0;

/// بين صفوف البيانات داخل بطاقة الملخّص.
const kGapRow = 5.0;

/// حشوة البطاقة — بدل حشوة `GlassCard` الافتراضية (18 × 16).
const kCardPad = EdgeInsets.symmetric(horizontal: 16, vertical: 9);

/// ارتفاع الزرّ الرئيسي في هذا المسار. لا ينزل تحت 48، وهو الحدّ الذي يصير
/// الزرّ دونه صعب الإصابة بالإبهام.
const kButtonHeight = 50.0;
