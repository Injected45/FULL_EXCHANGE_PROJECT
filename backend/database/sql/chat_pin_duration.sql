/* ============================================================================
   تثبيت الرسالة **لمدّة** لا إلى الأبد (أمر المالك، 6 سبتمبر 2026:
   «تثبيت في أعلى المحادثة لمدة يوم · أسبوع · شهر»).

   ⚠ طبقة تواصل لا طبقة مالية — كسابقاتها.

   `pinned_until` عمودٌ ثانٍ إلى جانب `pinned_at`، لا استبدالٌ له:
   الأوّل يقول **متى ثُبِّتت** (للترتيب حين تتعدّد المثبَّتة)، والثاني
   **متى ينتهي التثبيت**. ودمجُهما في عمود واحد يفقد أحد السؤالين.

   والانتهاء يُقرأ ولا يُكتب: لا مهمّة دورية تمرّ لترفع التثبيت، بل
   الاستعلام يرشّح `pinned_until > now`. مهمّةٌ دورية لهذا الغرض تعني
   جدولةً على الخادم من أجل إخفاء سطر.
   ============================================================================ */

IF COL_LENGTH('dbo.chat_messages', 'pinned_until') IS NULL
BEGIN
    ALTER TABLE dbo.chat_messages ADD pinned_until DATETIME2 NULL;
END;
GO
