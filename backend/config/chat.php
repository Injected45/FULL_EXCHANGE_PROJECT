<?php

return [

    /*
    |--------------------------------------------------------------------------
    | مفتاح دخول صندوق وارد الإدارة
    |--------------------------------------------------------------------------
    |
    | يُضبط في `.env` بالمفتاح `CHAT_ADMIN_KEY`. وبلا ضبطه لا تُفتح الصفحة
    | إطلاقاً — لا مفتاحَ افتراضياً عمداً: مفتاحٌ افتراضي في الشيفرة يعني أن
    | كل من قرأ المستودع يدخل صندوق وارد الإدارة.
    |
    | ولّده عشوائياً وطويلاً، مثلاً:
    |     php -r "echo bin2hex(random_bytes(24));"
    |
    | ⚠ مفتاح مشترك لا هويّة شخصية — انظر توثيق `AdminChatController`.
    |
    */
    'admin_key' => env('CHAT_ADMIN_KEY', ''),

];
